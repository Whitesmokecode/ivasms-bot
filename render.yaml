services:
  - type: web
    name: ivasms-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python ivasms_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0